#################################### common config : ####################################
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: fanshen,666.
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      format_sql: true
    show-sql: true


logging:
  level:
    root: info
    com.zfans: debug
  file:
    path: log/blog-dev.log

server:
  port: 8080

